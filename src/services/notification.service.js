const getStreamClient = require('../config/stream');

const client = getStreamClient();

const createNotification = async (channel, notification) => {
  // return client.sendUserMessage(notification, channel);
};
